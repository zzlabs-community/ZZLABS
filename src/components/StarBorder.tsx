"use client";import{ReactNode}from"react";import"./StarBorder.css";export default function StarBorder({children,href,color="#adff43",speed="5s",className=""}:{children:ReactNode;href:string;color?:string;speed?:string;className?:string}){return <a href={href} className={`star-border-container ${className}`}><div className="border-gradient-bottom" style={{background:`radial-gradient(circle,${color},transparent 10%)`,animationDuration:speed}}/><div className="border-gradient-top" style={{background:`radial-gradient(circle,${color},transparent 10%)`,animationDuration:speed}}/><div className="inner-content">{children}</div></a>}